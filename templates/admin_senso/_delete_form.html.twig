<form method="post" action="{{ path('app_admin_senso_delete', {'id': admin_senso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_senso.id) }}">
    <button class="btn">Delete</button>
</form>
